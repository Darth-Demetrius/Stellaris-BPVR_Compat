# record_type: object
# path:
# supported_version:
# upstream_status: new-upstream
# sort_key: [1, 5, "zone_kuat_industry"]
# sources:
#   path: bpvr_compat/common/zones/kuat_zones_replace_2461014769.txt
#   supported_version: 4.*
#
#   path: bpvr_fix/common/zones/kuat_zones_replace_2461014769.txt
#   supported_version: 4.2.4

zone_kuat_industry = {
	icon = GFX_district_specialization_trade
	base_buildtime = @zone_buildtime

	# Global flag to check if the Kuat mod is installed
	potential = {
		has_global_flag = install_kuat_mod
	}


	unlock = { uses_district_set = Eternal_city }

	resources = {
		category = planet_zones
		cost = {
			minerals = @zone_cost
		}
	}

	planet_modifier = {
		zone_building_slots_add = @BPV_ZONE_SLOT
	}

	triggered_district_planet_modifier = {
		potential = {
			exists = owner
			owner = { is_regular_empire = yes }
			NOT = { has_planet_flag = has_building_kuat_command_center_auto }
		}
		job_entertainer_add = 300
		job_resort_worker_add = 300
		job_trader_add = 300
		planet_housing_add = 900
	}

	zone_sets = {
		urban
	}

	max_buildings = @BPV_ZONE_SLOT

	include = {
		all
	}

	triggered_planet_modifier = {
		potential = {
			exists = owner
			owner = {
				is_regular_empire = yes
				has_active_tradition = tr_mercantile_commercial_enterprise
			}
			NOT = { has_planet_flag = has_building_kuat_command_center_auto }
		}
		job_trader_add = 100
		planet_housing_add = 100
		mult = 0.50
	}


	excluded_building_sets = {
		urban_automation
		generator_automation
		mining_automation
		farming_automation
	}
}
